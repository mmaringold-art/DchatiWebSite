/* Dchati landing — public runtime config (no secrets).
   Edit the values between the EDITMODE markers. */
window.DCHATI_CONFIG = /*EDITMODE-BEGIN*/{
  "accentHue": 245,
  "particleDensity": 20,
  "whatsappNumber": ""
}/*EDITMODE-END*/;
